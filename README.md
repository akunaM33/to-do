# React + Vite

## todo app  
Displays a list of to-do things.  
To install setup a Vite project using a react template: 
_npm create vite@latest ./ -- --template react_ 
Then install dependencies with _npm install_ 
_npm run dev_ to start Vite server 
